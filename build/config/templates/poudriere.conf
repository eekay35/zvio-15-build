NO_ZFS=${no_zfs}
ZPOOL=${ZPOOL}
ZROOTFS=${ZROOTFS}
RESOLV_CONF=/etc/resolv.conf
BASEFS=${OBJDIR}/ports
USE_PORTLINT=no
USE_TMPFS=yes
GIT_URL=${ports_repo}
GIT_BRANCH=${ports_branch}
DISTFILES_CACHE=${DISTFILES_CACHE}
ALLOW_MAKE_JOBS=yes
#MUTUALLY_EXCLUSIVE_BUILD_PACKAGES="llvm* rust* gcc* node*"
#PACKAGE_FETCH_WHITELIST="gcc* llvm* node* rust*"
PACKAGE_FETCH_BRANCH=latest
PACKAGE_FETCH_URL=https://pkg.FreeBSD.org/\${ABI}
RESTRICT_NETWORKING=no
BUILD_AS_NON_ROOT=no
